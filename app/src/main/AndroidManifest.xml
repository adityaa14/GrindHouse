<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.nmims.b1.miniproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".coffee" />
        <activity android:name=".cappucino" />
        <activity android:name=".End_Page" />
        <activity android:name=".tea" />
        <activity android:name=".calctea" />
        <activity android:name=".juice" />
        <activity android:name=".calcjuice" />
        <activity android:name=".linker" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignupActivity"></activity>
        <activity android:name=".HelpSupport"></activity>
    </application>

</manifest>